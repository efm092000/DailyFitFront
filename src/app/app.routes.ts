import { Routes } from '@angular/router';
import {ExerciseSearchComponent} from "./components/exercise-search/exercise-search.component";

export const routes: Routes = [
  { path: '', component: ExerciseSearchComponent}
];
